import { expect } from 'chai';
import EventEmitter from 'events'

describe('2 Reacting with RxJS', () => {
  describe('2.x ..', () => {
    describe('2.x.x xx', () => {
      it('should..', (done) => {

      });
    });
  });
});