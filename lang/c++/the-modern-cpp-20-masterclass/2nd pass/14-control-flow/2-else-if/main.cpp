#include <iostream>
#include <bitset>

int main()
{
    const unsigned char sign {'a'};

    if(sign == '0') {

    } else if (sign == 'a') {

    } else if (sign == 'b') {

    } else {

    }

    return 0;
}