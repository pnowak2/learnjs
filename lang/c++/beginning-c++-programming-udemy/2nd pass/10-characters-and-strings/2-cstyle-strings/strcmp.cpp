#include <iostream>
#include <iomanip>
#include <cctype>
#include <cstring>

using namespace std;

int main()
{

    cout << "strings are the same or not: " << strcmp("buba", "baba") << endl;
    cout << "strings are the same or not: " << strcmp("buba", "buba") << endl;

    return 0;
}