#include <iostream>

using namespace std;

int main()
{
    int score{0};

    if (score >= 0 && score <= 100)
    {
        if (score >= 0 && score <= 100)
        {
        }
    }
    else
    {
        if (score >= 0 && score <= 100)
        {
        }
    }

    return 0;
}