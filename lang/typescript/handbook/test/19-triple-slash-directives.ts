import { expect } from 'chai';
import * as sinon from 'sinon';

/// <reference path="..." />

describe('Triple Slash Directives', () => {
  describe('The compiler performs a preprocessing pass on input files to resolve all triple-slash reference directives. During this process, additional files are added to the compilation.', () => {

  });
});