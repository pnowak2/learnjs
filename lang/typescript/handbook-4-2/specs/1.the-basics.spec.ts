describe("1 Basic Types", () => {
    describe("Boolean", () => {
        it("should declare it", () => {
            let msg: string = "BUBA";
            expect(msg.toLowerCase()).toEqual('buba');
        })
    });
})