import { expect } from 'chai';
import { StringValidator } from './modules/validation';
import { ZipCodeValidator } from './modules/exporting-declaration';
import { EmailValidator, ElectronicMailValidator } from './modules/exporting-statements';
import { RegexpEmailValidator, RegexpZipCodeValidator } from './modules/exporting-reexporting';
// import { EmailValidator, ZipCodeValidator, ElectronicMailValidator, numberRegexp } from './modules/exporting-reexporting-all';

describe('Modules', () => {
  describe('Exporting', () => {
    it('should use exported content', () => { });
  });

  describe('Importing', () => {
    it('should use imported content', () => { });
  });
});
