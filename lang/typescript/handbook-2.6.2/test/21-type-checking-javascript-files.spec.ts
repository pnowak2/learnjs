import { expect } from 'chai';

describe('Type Checking JavaScript Files', () => {
  describe('TypeScript 2.3 and later support a mode of type-checking and reporting errors in .js files with --checkJs', () => {
    it('should read the section', () => { });
  });
});
